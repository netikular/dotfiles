function notes

  echo "Help with notes"
  echo "nn - make a new note"
  echo "ne - find a note to edit by name"
  echo "nf - find a note to edit by full text search"
  echo "no - open a note in obsidian"
  echo "nt - find all pending todo's in notes"

end
